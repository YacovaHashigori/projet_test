{% extends 'base.html.twig' %}

{% block title %}Castle{% endblock %}

{% block body %}
    <h1>Castle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ castle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ castle.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ castle.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ castle.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('castle_index') }}">back to list</a>

    <a href="{{ path('castle_edit', {'id': castle.id}) }}">edit</a>

    {{ include('castle/_delete_form.html.twig') }}
{% endblock %}
